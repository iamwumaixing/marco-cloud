spring:
  datasource:
    druid:
      max-active: 15
      max-wait: 5000
      keep-alive: true
      connection-properties: socketTimeout=30000;connectTimeout=3000
      time-between-eviction-runs-millis: 20000
      test-while-idle: true